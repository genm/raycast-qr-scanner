{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "qr-scanner",
  "private": true,
  "title": "QR Scanner",
  "description": "Scan QR codes with your camera, from your screens, or from a clipboard image",
  "icon": "extension-icon.png",
  "author": "genm",
  "license": "MIT",
  "categories": [
    "Productivity"
  ],
  "keywords": [
    "qr",
    "scanner",
    "camera",
    "screen",
    "clipboard"
  ],
  "platforms": [
    "macOS"
  ],
  "engines": {
    "node": ">=22.22.2"
  },
  "allowScripts": {
    "esbuild@0.28.2": true,
    "fsevents@2.3.3": true
  },
  "commands": [
    {
      "name": "scan-camera",
      "title": "Scan QR from Camera",
      "description": "Open a camera preview and scan a QR code",
      "mode": "view"
    },
    {
      "name": "scan-screen",
      "title": "Scan QR from Screen",
      "description": "Find QR codes visible on any connected display",
      "mode": "view"
    },
    {
      "name": "scan-clipboard",
      "title": "Scan QR from Clipboard",
      "description": "Find QR codes in copied images",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "2.1.0",
    "@raycast/utils": "2.3.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "2.2.0",
    "@types/node": "22.19.17",
    "@types/react": "19.0.10",
    "eslint": "10.0.3",
    "prettier": "3.9.0",
    "typescript": "6.0.3",
    "vitest": "4.1.11"
  },
  "scripts": {
    "build": "ray build -e dist",
    "prebuild": "node scripts/check-swift-build-lock.mjs",
    "check": "npm run check:error-contract && npm run lint && npm test && npm run test:swift && npm run build && npm run audit:dependencies",
    "check:error-contract": "node scripts/check-error-code-contract.mjs",
    "audit:dependencies": "npm audit --omit=dev --audit-level=high",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "test": "vitest run --reporter=json --outputFile=test-results/vitest.json",
    "test:swift": "mkdir -p test-results && swift test --package-path swift --parallel --xunit-output test-results/swift.xml"
  }
}
